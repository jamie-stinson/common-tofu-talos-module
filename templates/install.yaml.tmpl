machine:
  install:
    disk: /dev/sda
  systemDiskEncryption:
    ephemeral:
      provider: luks2
      keys:
        - nodeID: {}
          slot: 0
    state:
      provider: luks2
      keys:
        - nodeID: {}
          slot: 0
  network:
    hostname: ${hostname}
  time:
    servers:
      - ${ntp_server}
